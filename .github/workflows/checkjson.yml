name: checkjson
run-name: Committed by @${{ github.actor }}
on: [push]
jobs:
  check-json-formatting:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - run: echo Hello from Workflow